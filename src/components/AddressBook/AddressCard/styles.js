import { BinIcon } from '../../../ui-library/BinIcon';
import styled from 'styled-components';

export const DeleteIcon = styled(BinIcon)`
  position: absolute;
  top: 0.75em;
  right: 0.75em;
  padding: 0.25em;
  cursor: pointer;
`;
